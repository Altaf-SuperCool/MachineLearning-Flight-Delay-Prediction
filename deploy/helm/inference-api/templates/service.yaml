apiVersion: v1
kind: Service
metadata:
  name: inference-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: inference-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
